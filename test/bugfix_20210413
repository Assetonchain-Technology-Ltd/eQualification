bugfix - 13 April 2021

WorkerProfielToken.sol
1. createWorkerProfile - add checking on balanceOf individual==0 before mint. 
2. createWorkerProfile - change order of return and emit
3. createWorkerProfile - address cast to uint256 change to uint256(uint160(address))

WorkerProfielLogic.sol
1. add function setStatus
2. add fucntion getStatus
3. _hasERC721Token - change return (_owner==token.ownerOf(_a)) to  return (_owner==token.ownerOf(_tokenid))
4. addAttribute - add back require(ab.exists(_key),"WL05")

QualificationToken.sol
1. grantQualification , change to first setSubnodeRecord in the orgens , then setAddr in public resolver.
2. grantQualification - address cast to uint256 change to uint256(uint160(address))
3. grantQualification - add a string _label para for register ens name, as using wallet address of casting the wallet address to int will result a different namehash due to datatype  
4. grantQualification - add ens.setOwn at the end to return back the ownership of subnode to node's owner.  

Feature:

WorkerProfielToken.sol
1. createWorkerProfile -Revoke change to register ENS after token is mint to _individual


